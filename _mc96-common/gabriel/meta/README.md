# _mc96-common/gabriel

**Source of truth:** `NOIZYANTHROPIC` monorepo on GOD.local, maintained by RSP_001.
**Synced:** 2026-04-20T17:47:36Z via `ops/gabriel-to-all-brands.sh`.
**One for all, all for one** — this payload appears identically in every RSPNOIZY brand repo so any brand can pull shared Gabriel doctrine + source + UI + agents + skills + rules without cross-repo dependencies.

## Layout

- `doctrine/` — 15 canonical MDs (MANIFESTO, MISSION, CHARACTER, SAFETY_CONTRACT, VISION, etc.)
- `source/` — GABRIEL CLI TypeScript (daemon + bosses + voice + gabriel + character)
- `scripts/` — install-launch-agent etc.
- `reference/` — captured Claude Agent SDK reference (custom tools, MCP, tool search, subagents, hosting, secure deployment, TS V2, Python, migration)
- `mcp/` — Gabriel MCP server (single-file stdio)
- `ios/` — Gabriel iOS app source
- `ui/` — Gabriel dashboard JS/CSS + public assets
- `agents/` — Claude subagent definitions (orchestrator, morning, review)
- `skills/gabriel-ops/` — progressive-disclosure ops skill
- `rules/` — 4 canonical rules (agents, consent-kernel, voice-pipeline, deployment)

## Doctrine
RSP_001 · rsp@noizy.ai · 75/25 royalty · Never Clauses immovable · 396 Hz.
